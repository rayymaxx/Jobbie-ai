# 🤖 Job Assistant Frontend (Streamlit)

## Setup and Run

```bash
cd job-assistant/frontend
python -m venv .venv 
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r ../requirements-frontend.txt
streamlit run streamlit_app.py